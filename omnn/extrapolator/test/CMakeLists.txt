SET (this_target test_extrapolator)
project(${this_target})
file(GLOB src *.cpp)
foreach(TEST ${src})
    get_filename_component(TEST_NAME ${TEST} NAME_WE)
    add_executable(${this_target}_${TEST_NAME} ${TEST_NAME})
    target_link_libraries(${this_target}_${TEST_NAME} extrapolator boost_test_exec_monitor-mt)
    add_test(NAME ${TEST_NAME} COMMAND ${TEST})
endforeach()